---
description: 変更をコミットする（git status, git add）
---

// turbo-all

1. `git status` を実行して変更されたファイルを確認します。
2. `git add .` を実行して全ての変更・新規ファイルをステージングします。
3. 変更内容に適したコミットメッセージを作成し、`git commit -m "..."` を実行してよいか、ユーザーに確認します。